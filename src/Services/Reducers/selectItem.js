const initailSelect = null;

const selectItem = (state = initailSelect, action) => {
  switch (action.type) {
    case "CLICK_ITEM":
      return this.state;

    default:
      return state;
  }
};

export default selectItem;
